# deps + generated
node_modules/
styled-system/

# build output
dist/
.output/
.nitro/
.tanstack/

# cloudflare / alchemy (generated per deploy; wrangler.jsonc carries the
# resolved secret values, so it must never land in git)
.alchemy/
.wrangler/
wrangler.jsonc

# env + os (the example ships as the template for real .env files)
.env
.env.*
!.env.example
.DS_Store
*.log

# vite dev cache
.vite/
